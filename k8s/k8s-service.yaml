# Service — gives the pods a stable network endpoint
apiVersion: v1
kind: Service
metadata:
  name: medical-cv-serve
  namespace: ml
spec:
  type: ClusterIP
  selector:
    app: medical-cv-serve
  ports:
    - name: http
      port: 80
      targetPort: 8000
